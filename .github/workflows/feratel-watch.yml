name: Feratel Watch
on:
  workflow_dispatch: {}
  schedule:
    - cron: "*/10 * * * *"  # every 10 minutes

permissions:
  contents: read
  actions: write

jobs:
  sanity:
    runs-on: ubuntu-latest
    steps:
      - run: echo "watch workflow loaded"
